<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rydeit.io">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Rydeit">
        <activity android:name=".ui.WebViewActivity"></activity>
        <activity android:name=".ui.ResetPasswordActivity" />
        <activity android:name=".ui.RegisterStep3Activity" />
        <activity android:name=".ui.RegisterStep2Activity" />
        <activity android:name=".ui.RegisterStep1Activity" />
        <activity android:name=".ui.ForgetPasswordActivity" />
        <activity android:name=".ui.PinActivity" />
        <activity android:name=".ui.Login2faActivity" />
        <activity android:name=".ui.LoginActivity" />
        <activity
            android:name=".ui.MainActivity"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustNothing">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>